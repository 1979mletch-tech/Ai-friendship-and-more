import{describe,expect,it}from'vitest';import{AURORA_PRESENTATION_AGE,MINIMUM_USER_AGE,adultGateCopy,ageEligibility,mayUseInteractiveAurora}from'./agePolicy';describe('adult-only policy',()=>{it('is 18+',()=>expect(MINIMUM_USER_AGE).toBe(18));it('blocks 17',()=>expect(ageEligibility(17)).toBe('underage'));it('allows 18',()=>expect(ageEligibility(18)).toBe('eligible'));it('treats missing age as unknown',()=>expect(ageEligibility(null)).toBe('unknown'));it('requires confirmation',()=>expect(mayUseInteractiveAurora(false)).toBe(false));it('presents Aurora as adult',()=>expect(AURORA_PRESENTATION_AGE).toBe('25+'));it('has clear copy',()=>expect(adultGateCopy).toContain('18+'))})