const safeMessages=new Set(['Your session has expired. Please sign in again.','Server API is not configured.','Conversation request failed.','Companion profile request failed.','Billing request failed.','Privacy request failed.','Invalid conversation identifier.'])
export const userSafeError=(error:unknown,fallback='Something went wrong. Please try again.')=>{if(error instanceof DOMException&&error.name==='AbortError')return'The request took too long. Please try again.';if(error instanceof TypeError)return'Unable to reach the service. Check your connection and try again.';if(error instanceof Error&&safeMessages.has(error.message))return error.message;return fallback}
