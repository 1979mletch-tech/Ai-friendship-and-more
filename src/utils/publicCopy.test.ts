import{describe,expect,it}from'vitest';import{AI_DISCLOSURE,PREVIEW_DISCLOSURE,footerCopy}from'./publicCopy';describe('public copy',()=>{it('discloses AI identity',()=>expect(AI_DISCLOSURE).toContain('AI companion'));it('does not overclaim cloud',()=>expect(PREVIEW_DISCLOSURE).toContain('after verification'));it('keeps emergency boundary',()=>expect(footerCopy()).toContain('Not an emergency service'))})