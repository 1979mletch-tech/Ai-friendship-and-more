import{describe,expect,it}from'vitest';import{marketingCopyIsSafe,marketingSafetyIssues}from'./marketingSafety';describe('marketing safety',()=>{it('accepts ordinary brand copy',()=>expect(marketingCopyIsSafe('An AI companion for creativity')).toBe(true));it('blocks dependency claim',()=>expect(marketingCopyIsSafe('An AI that never leaves you')).toBe(false));it('reports unsafe patterns',()=>expect(marketingSafetyIssues('Guaranteed private')).toHaveLength(1))})