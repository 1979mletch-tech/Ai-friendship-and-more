import{describe,expect,it}from'vitest';import{appendExchange,newLocalConversation,selectConversation}from'./chatPersistence'
describe('chat persistence',()=>{it('keeps messages with their conversation',()=>{const a=appendExchange(newLocalConversation('a'),'hello','hi',new Date('2026-01-01'));const b=newLocalConversation('b');expect(selectConversation([a,b],'a')?.messages).toHaveLength(2);expect(selectConversation([a,b],'b')?.messages).toHaveLength(0)});it('titles first exchange',()=>expect(appendExchange(newLocalConversation('a'),'My project idea','ok').title).toBe('My project idea'))})
