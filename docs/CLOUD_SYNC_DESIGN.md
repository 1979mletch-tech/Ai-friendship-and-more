# Cloud Sync Design

Current cloud controls provide explicit backup foundations. A full sync engine should later define conflict resolution, pagination, multi-device ordering, offline behavior, deduplication and deletion propagation.

Until then, UI/docs should say backup rather than imply seamless real-time synchronization.
