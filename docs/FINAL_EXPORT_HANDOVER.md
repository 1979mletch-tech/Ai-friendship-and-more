# Final Export Handover

A final ZIP should identify its source commit and include source/config/tests/Supabase/docs/.env.example while excluding secrets/dependencies/build caches.

Before handover, independently extract the ZIP in a clean environment, install from lockfile, run lint/typecheck/tests/build, and compare key expected files. Record that evidence.
