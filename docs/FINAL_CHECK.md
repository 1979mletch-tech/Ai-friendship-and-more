# Final Check

Before handover confirm: correct repository/product, clean PR scope, exact-head CI, no secrets, documented external configuration, known limitations, staging evidence status, export plan and explicit human merge/release authorization.

AI Friendship should remain separate from every other product repository/workstream.
