# Current Staging Candidate

This file records intent, not a verification claim.

Candidate branch: `chatgpt/ai-friendship-build`

A candidate becomes the staging baseline only when its exact head SHA has:
- successful quality-gate CI,
- successful preview deployment,
- a recorded tappable deployment URL.

Cloud/auth/AI features remain CODE BUILT until configured and exercised live. Two-account isolation remains NOT LIVE VERIFIED until the matrix is executed against the deployed backend.
