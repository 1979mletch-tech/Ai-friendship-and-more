# Preview User Notice

A test preview should clearly indicate that it is non-production and should use synthetic/non-sensitive data. Cloud/AI features that are not configured should say so rather than presenting fake success.

Remove test labeling only when moving through the deliberate production release process.
