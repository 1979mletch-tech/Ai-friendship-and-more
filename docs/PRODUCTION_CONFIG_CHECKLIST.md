# Production Configuration Checklist

Confirm exact values outside source control for hosting origin, Supabase project, auth redirects, Edge Function secrets, allowed origin, AI model/provider, retention settings and any billing webhook configuration.

Store only variable names/examples in the repository. Verify configuration separately in staging and production.
