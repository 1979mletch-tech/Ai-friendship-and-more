# User Trust Rules

Prefer transparent product behavior:
- say when AI is unavailable;
- say when data is local-only;
- say when cloud features are not configured;
- distinguish proposed pricing from active billing;
- expose deletion/export controls;
- never invent test results, endorsements or provider guarantees.

Trust copy must follow evidence.
