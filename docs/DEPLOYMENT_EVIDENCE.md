# Deployment Evidence

For every preview/staging deployment record the exact source commit, deployment identifier, HTTPS URL, environment type, configured feature flags/public variable names, Edge Function version and migration state.

Do not infer deployment identity from repository state alone.
