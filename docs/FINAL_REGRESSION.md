# Final Regression

After the last functional fix: freeze the SHA, run CI, deploy that same SHA, rerun core journeys, safety/adversarial checks, two-account isolation, privacy/export/delete, mobile/accessibility smoke, and verify known limitations.

Any code change after the frozen SHA requires the relevant final regression to run again.
