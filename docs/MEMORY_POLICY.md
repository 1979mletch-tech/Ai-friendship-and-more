# Memory Policy

Long-term memory is user-controlled.

- remember only user-approved items;
- provide view/forget/clear controls;
- bound item length/count;
- keep memory distinct from raw conversation history;
- do not use memory to manipulate dependency;
- do not expose one user's memory to another;
- honor account/data deletion semantics.

Cloud memory isolation requires live two-account verification.
