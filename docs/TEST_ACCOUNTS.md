# Synthetic Test Accounts

Use dedicated synthetic addresses created for staging. Never reuse a real user's credentials or production conversation content.

Record only synthetic account identifiers needed for evidence. Do not commit passwords, recovery links, access tokens or refresh tokens.

After testing, delete synthetic accounts and verify cleanup where required.
