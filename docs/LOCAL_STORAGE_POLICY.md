# Local Storage Policy

Local storage is used for preview/convenience state and may be read by scripts running in the same origin.

Do not store provider secrets, service-role keys, passwords or intentionally persist raw auth credentials beyond the auth session envelope required by the current client.

Users receive local clear/export controls. Production privacy copy must accurately describe browser-local data.
