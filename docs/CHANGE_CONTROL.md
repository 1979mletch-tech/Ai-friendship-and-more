# Change Control

After release-candidate freeze, every code/config/schema/provider change is recorded and assessed for which gates must rerun. Safety, authorization, secrets, billing and data-deletion changes require focused regression in addition to general CI.
